<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        Kakao OAuth redirect handler.

        The Kakao SDK ships an `AuthCodeCustomTabsActivity` that must receive
        the OAuth callback via a scheme `kakao<native_app_key>`. We expose
        the native key via Gradle's `manifestPlaceholders` block (see
        app/build.gradle) so CI / release builds substitute the real key
        while local dev uses the 0-padded placeholder defined in
        app/build.gradle. Never commit a real key here.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required for the reading-timer foreground service (API 28+). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required on API 34+ to declare the foreground service type as dataSync. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Keeps the CPU alive while the timer counts seconds in the background. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Centers the "근처 모임" search on the device location (M66). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Agora RTC video call (M71): camera + microphone capture plus the audio
         routing / network-state access the engine needs. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:label="골방"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Kakao OAuth custom-tabs redirect -->
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeCustomTabsActivity"
            android:exported="true">
            <intent-filter android:label="KakaoOAuth">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Scheme is `kakao<NATIVE_APP_KEY>`. Substituted at build
                     time via manifestPlaceholders (see app/build.gradle). -->
                <data android:scheme="kakao${kakaoNativeAppKey}"
                      android:host="oauth" />
            </intent-filter>
        </activity>

        <!-- flutter_background_service foreground service for the reading timer.
             The service type `dataSync` satisfies API 34+ foreground-type
             requirements; FOREGROUND_SERVICE_DATA_SYNC permission declared above. -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
